import 'package:flutter/material.dart';

class DBProfilePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("profile page"),
      ),
    );
  }
}
